package cat.receptari.app.domain.model

/** A curated set of pictograms a folder can be marked with — not a free image upload. */
enum class FolderIcon {
    FOLDER,
    RESTAURANT,
    CAKE,
    COOKIE,
    PIZZA,
    ICE_CREAM,
    BAKERY,
    COFFEE,
    SOUP,
    GRILL,
    SEAFOOD,
    VEGETARIAN,
    CELEBRATION,
    FAMILY,
}
