<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Compose owns all real styling. This theme only covers the window before the first
        frame, so it just needs a matching background and no action bar.
    -->
    <style name="Theme.Receptari" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
